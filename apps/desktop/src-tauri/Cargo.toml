[package]
name = "hawkvance"
version = "0.1.0"
description = "HawkVance AI desktop"
authors = ["HawkVance"]
edition = "2021"
rust-version = "1.77"

[lib]
# Named apart from the binary so the same code can be unit tested without the
# Tauri runtime standing up a window.
name = "hawkvance_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# No asset protocol: the frontend is bundled into the binary and files are read through
# commands, so exposing a file URL scheme to the web view would widen it for nothing.
tauri = { version = "2" }
tauri-plugin-dialog = "2"
tauri-plugin-updater = "2"

serde = { version = "1", features = ["derive"] }
serde_json = "1"

# The vault. SQLCipher is bundled, and so is the OpenSSL it needs: without the
# vendored variant the build demands an OPENSSL_DIR on every machine that ever
# compiles this, which is not a reasonable thing to ask of a contributor and not
# something a release pipeline should depend on finding.
rusqlite = { version = "0.32", features = ["bundled-sqlcipher-vendored-openssl", "chrono"] }

aes-gcm = "0.10"
rand = "0.8"
base64 = "0.22"
sha2 = "0.10"
hex = "0.4"

reqwest = { version = "0.12", features = ["json", "rustls-tls"], default-features = false }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "process", "io-util", "sync", "time"] }

chrono = { version = "0.4", features = ["serde"] }
thiserror = "2"
sysinfo = "0.33"
uuid = { version = "1", features = ["v4", "serde"] }

[target.'cfg(windows)'.dependencies]
# The master key lives in Windows Credential Manager, never on disk beside the vault.
windows = { version = "0.58", features = [
  "Win32_Foundation",
  "Win32_Security_Credentials",
] }

[features]
# Tauri's own default. Kept explicit so a release build cannot silently pick up
# the development console.
custom-protocol = ["tauri/custom-protocol"]

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
